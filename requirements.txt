# All packages below are pre-installed in the Databricks Apps runtime.
# No PyPI access from the Apps container — only list what's already there.

fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
requests>=2.32.0
httpx>=0.27.0
pydantic>=2.10.0,<3.0.0
