# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# HTTP
requests==2.32.3
httpx==0.27.2

# Data
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# ── Databricks AI Dev Kit ─────────────────────────────────────────────────────
# Tools Core: sql, unity_catalog, serving, file, vector_search, apps, lakebase
databricks-sdk==0.32.0

# ── Document parsing ──────────────────────────────────────────────────────────
# Fallback for local dev only.
# In Databricks: ai_parse_document() handles PDF + DOCX natively.
python-docx==1.1.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
